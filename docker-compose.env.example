# Copy to .env next to docker-compose.yml (optional; defaults work for local trials).
# docker compose --env-file .env up --build

HTTP_PORT=8080
POSTGRES_PASSWORD=postgres
JWT_SECRET=replace-with-a-long-random-secret
